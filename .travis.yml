language: node_js
node_js:
  - "stable"
before_script:
  - yarn install
script:
  - yarn build
deploy:
  provider: pages
  skip_cleanup: true
  github-token: $GITHUB_TOKEN
  target_branch: gh-pages
  local_dir: docs
  on:
    branch: master
