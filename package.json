{
  "name": "eliancodes-frontend",
  "version": "0.5.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "pnpm build && firebase deploy",
    "deploy:hosting": "pnpm build && firebase deploy --only hosting",
    "deploy:functions": "npm run deploy --prefix functions --only functions",
    "prerelease": "pnpm build && firebase hosting:channel:deploy canary"
  },
  "devDependencies": {
    "@astrojs/partytown": "^1.0.0",
    "@astrojs/rss": "^1.0.0",
    "@astrojs/sitemap": "^1.0.0",
    "@tailwindcss/typography": "^0.5.4",
    "astro": "1.0.1",
    "autoprefixer": "^10.4.8",
    "postcss": "^8.4.16",
    "postcss-import": "^14.1.0",
    "sass": "^1.54.4",
    "tailwindcss": "^3.1.8"
  },
  "dependencies": {
    "firebase": "^9.9.2"
  }
}